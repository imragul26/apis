with:
    file_glob: "specifications/**/*.{yaml,yml}"
# apis

The repository is renamed to Investor Maintenance. Could you please add the repository to Stoplight? Once it is added, could you please open the project in Edit mode from the Stoplight UI and commit the generated .spectral.mjs file?

I have also created a PR to add the Spectral linting GitHub Action. Once the .spectral.mjs file is committed, we can verify the linting through the PR check.
